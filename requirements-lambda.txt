# Lambda runtime dependencies — mcp and dev tools excluded (not needed in Lambda)
langgraph>=0.2.0
langchain-aws>=0.2.0
boto3>=1.35.0
pydantic>=2.0
pydantic-settings>=2.0
structlog>=24.0
aws-lambda-powertools>=3.0
python-dotenv>=1.0
